#!/usr/bin/env bash
set -euo pipefail

APP_DIR="/usr/lib/scrobload"
VENV_DIR="/var/lib/scrobload/venv"

if [[ -x "${VENV_DIR}/bin/python" ]]; then
  exec "${VENV_DIR}/bin/python" "${APP_DIR}/app.py" "$@"
fi

exec /usr/bin/python3 "${APP_DIR}/app.py" "$@"
